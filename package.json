{
  "name": "table-chess",
  "version": "1.0.0",
  "description": "Local multiplayer chess platform - host on one computer, play from any tablet on the network",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "lint": "eslint .",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "chess.js": "^1.0.0",
    "express": "^4.19.2",
    "selfsigned": "^5.5.0",
    "ws": "^8.17.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.7.0"
  }
}
